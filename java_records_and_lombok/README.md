# Records and Lombok in Practise

Code examples to support medium article.

## Usage

Use the gradle wrapper to launch tests:

* Windows = `gradlew.bat [OPTIONS] commands`
* UNIX = `./gradlew [OPTIONS] commands`
